  <tr class="favorite_table_row" id="remove_favorite_<%= favorite.id %>">
    <td><%= link_to Resource.find_by_id(favorite.resource_id).title, Resource.find_by_id(favorite.resource_id) %></th>
    <td>
        <%= link_to "Remove", {:controller => 'favorites', :action => 'destroy', :format => :js, :id => favorite.id}, :remote => true %>
  </tr>
